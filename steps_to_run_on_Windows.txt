Steps to run news agent on Windows:
cd News_Agent
set PYTHONPATH=backend
uv run python -m uvicorn research_engine.app:app --host 0.0.0.0 --port 2024 --reload

Then open another terminal and run:
cd frontend
npm run dev